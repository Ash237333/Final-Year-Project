import Dataloader
from Model import Transformer

EPOCHS = 20
train, test = Dataloader.load_dataset()

def train_one_epoch():
    return

def train():
    for epoch in range(EPOCHS):
        train_one_epoch()
    return
