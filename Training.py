import Dataloader
from Model import Transformer

EPOCHS = 20
train, test = Dataloader.create_dataloader()

def train_one_epoch():
    return

def train():
    for epoch in range(EPOCHS):
        train_one_epoch()
    return
